import Foundation

enum AccessibilityIdentifiers {
    // MARK: - Dashboard
    static let dashboardHeroCard = "dashboard.heroCard"
    static let dashboardUpcomingShift = "dashboard.shift.upcoming"
    static let dashboardActiveShift = "dashboard.shift.active"
    static let dashboardStartShift = "dashboard.action.startShift"
    static let dashboardLogBreak = "dashboard.action.logBreak"
    static let dashboardAddShift = "dashboard.action.addShift"

    // MARK: - Onboarding
    static let onboardingProgress = "onboarding.progress"
    static let onboardingPrimary = "onboarding.primary"
    static let onboardingBack = "onboarding.back"
    static let onboardingSkip = "onboarding.skip"

    // MARK: - Schedule
    static let scheduleAddShift = "schedule.addShift"
    static let scheduleCalendarStrip = "schedule.calendarStrip"

    // MARK: - Hours Dashboard
    static let hoursHeroCard = "hours.heroCard"
    static let hoursSummaryCard = "hours.summaryCard"
    static let hoursChartCard = "hours.chartCard"
    static let hoursOvertimeCard = "hours.overtimeCard"
    static let hoursRateBreakdownCard = "hours.rateBreakdownCard"

    // MARK: - Settings
    static let settingsSubscription = "settings.subscription"
    static let settingsCalendar = "settings.calendar"
    static let settingsProfile = "settings.profile"

    // MARK: - Tab Bar
    static let tabDashboard = "tab.dashboard"
    static let tabSchedule = "tab.schedule"
    static let tabHours = "tab.hours"
    static let tabSettings = "tab.settings"
}
